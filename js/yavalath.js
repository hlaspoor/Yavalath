"use strict";

$(function () {
    var g = new Game();
    g.new_game();

    //var ai = new AI(g);
    //var mvs = g.generate_moves();
    //for (var i = 0; i < mvs.length; i++) {
    //    console.log(MOVE_NAME(mvs[i]));
    //}
});
